{
  "name": "jarvis-sample-node-app",
  "version": "1.0.0",
  "description": "Sample Node.js app containerized and deployed by autonomous DevOps pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/server.test.js",
    "build": "npm run build:frontend",
    "build:frontend": "npm --prefix frontend run build",
    "validate:python": "python -m compileall ai_engine intent_engine github_analyzer docker_builder k8s_deployer terraform_runner orchestrator cli self_healing monitoring",
    "validate:iac": "terraform -chdir=terraform fmt -check && terraform -chdir=terraform init -backend=false && terraform -chdir=terraform validate",
    "validate": "npm run test && npm run build && npm run validate:python"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "supertest": "^7.1.3"
  }
}
