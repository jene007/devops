apiVersion: v1
kind: Service
metadata:
  name: jarvis-app-service
  namespace: jarvis
spec:
  selector:
    app: jarvis-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer
