This submission demonstrates a complete automated DevOps implementation aligned to the rubric. For version control and collaboration, the repository includes a formal contribution policy, code ownership, PR template, and issue templates to enforce structured team workflow. For CI/CD, the GitHub Actions pipeline is split into a dedicated CI stage (tests, frontend build, Python checks, Terraform validation) and a gated deploy stage (Docker build/push, Terraform apply, Kubernetes rollout), with PR and main/master triggers configured. For containerization and deployment, the application is containerized with a hardened runtime (healthcheck and non-root execution), optimized build context, and Kubernetes deployment manifests. For Infrastructure as Code, Terraform definitions provision core AWS resources and are validated in pipeline before apply. Evidence mapping and verification instructions are documented in `cicd/RUBRIC_EVIDENCE.md`, with successful local validation (`npm run validate`) and GitHub Actions execution logs/screenshots attached for PR and main-branch runs.
